# Firebase Web App configuration. These values identify the public web client;
# they are not Firebase Admin credentials or server secrets. Data access is
# enforced by Firebase Authentication and firestore.rules.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
